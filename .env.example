# AWS Configuration
AWS_PROFILE=                    # Your AWS profile name (run: aws configure --profile your-profile)
AWS_REGION=eu-west-2           # AWS region for deployment
ACCOUNT_ID=123456789113        # Your AWS account ID (run: aws sts get-caller-identity --query Account --output text)

# Docker and Repository Settings
DOCKER_TAG=latest              # Docker image tag for builds
REPOS_FILE=infra/repos.yaml    # Path to repository configuration file

# API Keys (Required)
ANTHROPIC_API_KEY=             # Get from: https://console.anthropic.com/settings/keys
GITHUB_PERSONAL_ACCESS_TOKEN=  # Get from: https://github.com/settings/tokens (needs 'repo' scope)

# SSH Access (Optional - for debugging AWS instances)
SSH_KEY_PATH=~/.ssh/janitor-key # Path to private key (must be absolute on Windows)